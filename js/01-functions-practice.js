//STEP 1
/*function halfNumber(aNumber) {
    let half = aNumber / 2;
    document.write("Half of " + aNumber + " is " + half);
}
halfNumber(66);*/
//STEP 2
/*function squareNumber(aNumber) {
    let square = Math.pow(aNumber, 2);
    document.write(square + " is " + aNumber + " squared ");
}
squareNumber(66);*/

//STEP 3
/*function percentOf(num1, num2) {
    let percent = num1 * num2 / 100;
    document.write(num2 + " is " + percent + " percent of " + num1);
}
percentOf(100, 10);*/
//STEP 4
/*function percentOf(num1, num2) {
    let modulus = num1 % num2;
    document.write(modulus + " is the modulus of " + num1 + " and " + num2);
}
percentOf(66, 8);*/

//STEP 5
/*let num1 = window.prompt("Please enter a number");
let num2 = window.prompt("Please enter a number");
let num3 = window.prompt("Please enter a number");

function nums(num1, num2, num3) {
    let sum = 0;
    for (let i = 0; i < arguments.length; i++) {
        sum += +arguments[i];
    }
    document.write(sum);
}
nums(num1, num2, num3);*/
