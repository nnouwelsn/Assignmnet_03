/*function results() {
    let answer = window.prompt("Enter rock, paper or scissors");
    console.log(answer);


    function getRandomInt(max) {
        return Math.floor(Math.random() * Math.floor(max));
    }
    let computer = getRandomInt(3);
    console.log(computer);

    if (answer === "rock" && computer === 0) {
        alert("tie!");
        results();

    } else if (answer === "rock" && computer === 1) {
        alert("Rock vs. paper, you lose.");
        results();

    } else if (answer === "rock" && computer === 2) {
        alert("Rock vs. scissors, you win!");
        results();

    } else if (answer === "paper" && computer === 0) {
        alert("Paper vs. rock, you win!");
        results();

    } else if (answer === "paper" && computer === 1) {
        alert("tie!");
        results();

    } else if (answer === "paper" && computer === 2) {
        alert("Paper vs. scissors, you lose.");
        results();

    } else if (answer === "scissors" && computer === 0) {
        alert("Scissors vs. rock, you lose.");
        results();

    } else if (answer === "scissors" && computer === 1) {
        alert("Scissors vs. paper, you win.!");
        results();

    } else if (answer === "scissors" && computer === 2) {
        alert("tie!");
    } else {
        results();

    }
}
results();*/
